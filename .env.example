# 复制为 .env 并填入你的 key（.env 已被 gitignore，不会提交）

# 模型服务的 API key（OpenAI 兼容）。这里以硅基流动为例。
LLM_API_KEY=sk-xxxxxxxx

# OpenAI 兼容的接口地址。默认硅基流动；换其他家改这里即可：
#   硅基流动:   https://api.siliconflow.cn/v1
#   DeepSeek:   https://api.deepseek.com/v1
#   通义千问:   https://dashscope.aliyuncs.com/compatible-mode/v1
#   Kimi:       https://api.moonshot.cn/v1
LLM_BASE_URL=https://api.siliconflow.cn/v1

# 模型 ID（注意硅基流动用带前缀的 ID，如 deepseek-ai/DeepSeek-V4-Pro）
LLM_MODEL=deepseek-ai/DeepSeek-V4-Pro

# Web 服务可选：设置访问密码，保护接口不被滥用
# APP_PASSWORD=your-pass

# 可选：飞书文档读取（用户授权 / OAuth，读你账号有权限的文档）。
# 应用需开通 docx:document:readonly + wiki:wiki:readonly + offline_access，
# 并在「安全设置 → 重定向 URL」登记下面的 callback 地址。
# FEISHU_APP_ID=cli_xxxxxxxx
# FEISHU_APP_SECRET=xxxxxxxx
# FEISHU_REDIRECT_URI=http://你的IP:8000/feishu/callback
# 令牌持久化路径（默认 ~/.ai-test-copilot/feishu_token.json）
# FEISHU_TOKEN_FILE=/etc/ai-test-copilot.feishu-token.json
# 国际版 Lark：FEISHU_BASE=https://open.larksuite.com，FEISHU_ACCOUNTS_BASE=https://accounts.larksuite.com
